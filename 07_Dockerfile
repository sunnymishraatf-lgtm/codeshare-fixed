FROM node:20-alpine

WORKDIR /app

COPY package*.json ./

# FIX: --only=production is deprecated in npm v7+, replaced with --omit=dev
RUN npm ci --omit=dev

COPY . .

RUN mkdir -p uploads

EXPOSE 3000

CMD ["node", "server.js"]
